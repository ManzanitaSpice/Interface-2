REPORTE DE LIMPIEZA (archivos/carpetas posiblemente basura o prescindibles)

1) src/assets/react.svg
   - No se detectaron referencias activas en el código actual.
   - Probablemente remanente del template de Vite.

2) src-tauri/skin-storage/
   - Contiene archivos PNG de almacenamiento local runtime (datos generados en ejecución), no código fuente.
   - Conviene excluirlos del repo si no son fixtures de prueba.

3) node_modules/
   - Dependencias generadas localmente, no deben versionarse.
   - Mantener fuera de control de versiones (ya normal en la mayoría de proyectos).

4) Archivos de lock/caché de entorno local (verificar según flujo del equipo)
   - No se detectaron adicionales graves en esta revisión.


REPORTE DE ARCHIVOS GRANDES Y POSIBLE DIVISIÓN (estructura modular)

A) src/App.tsx (~3200+ líneas)
   - SI conviene dividir.
   - Recomendado separar en:
     - src/pages/* por sección principal (Mis Modpacks, Configuración Global, Novedades, Updates, Creador).
     - src/features/settings/* (idioma, apariencia, java, rutas)
     - src/features/instances/* (cards, panel derecho, tooltip/meta)
     - src/hooks/* para lógica de persistencia y side-effects.
   - Beneficio: mantenibilidad, testeo y menor riesgo de regresiones.

B) src/styles/components.css (~1700+ líneas)
   - SI conviene dividir.
   - Recomendado por dominio visual:
     - styles/components/import.css
     - styles/components/settings.css
     - styles/components/instances.css
     - styles/components/news-updates.css
   - Beneficio: menor colisión de estilos y mejor trazabilidad.

C) src-tauri/src/app/instance_service.rs (~3300+ líneas)
   - SI conviene dividir.
   - Recomendado en módulos internos:
     - instance_service/launch.rs
     - instance_service/metadata.rs
     - instance_service/process.rs
     - instance_service/io.rs
   - Beneficio: tiempos de revisión menores y límites de responsabilidad claros.

D) src-tauri/src/services/loader_installer.rs (~1500+ líneas)
   - SI conviene dividir.
   - Recomendado por loader/provider y fases (resolver, download, validate, install).

E) src-tauri/src/commands/import.rs (~1000+ líneas)
   - SI conviene dividir.
   - Recomendado:
     - commands/import/scan.rs
     - commands/import/detect.rs
     - commands/import/actions.rs
     - commands/import/copy.rs

F) src-tauri/Cargo.lock (muy grande)
   - NO dividir (archivo generado automáticamente por Cargo).
   - Debe permanecer íntegro.

G) src-tauri/icons/icon.icns
   - NO dividir (asset binario final).

